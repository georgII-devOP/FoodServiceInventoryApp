<UserControl x:Class="FoodServiceInventoryApp.Views.ProductRemovalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:FoodServiceInventoryApp.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Background="White"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Удалить продукт" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>

            <ui:TextBox PlaceholderText="Название продукта для удаления" Margin="0 0 0 10"
                        Text="{Binding ProductNameToRemove, UpdateSourceTrigger=PropertyChanged}"/>

            <ui:TextBox PlaceholderText="Количество для удаления" Margin="0 0 0 10"
                        Text="{Binding QuantityToRemove, UpdateSourceTrigger=PropertyChanged}"/>

            <ui:TextBox PlaceholderText="Ед. Измерения" Margin="0 0 0 10"
                        Text="{Binding UnitOfMeasureToRemove, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0 10 0 0"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" Margin="0 10 0 0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
                <ui:Button Content="Удалить" Appearance="Danger" Margin="0 0 10 0" Command="{Binding RemoveProductCommand}"/>
                <ui:Button Content="Отмена" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>