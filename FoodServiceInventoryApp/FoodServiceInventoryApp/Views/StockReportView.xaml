<UserControl x:Class="FoodServiceInventoryApp.Views.StockReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:FoodServiceInventoryApp.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Background="White"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Текущий остаток на складе" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 20">
            <TextBlock Text="Фильтр по категории:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <ComboBox ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategoryFilter}"
                      Width="150" Margin="0 0 20 0">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>

            <ui:TextBox PlaceholderText="Поиск по названию" Width="200"
                        Text="{Binding ProductNameFilter, UpdateSourceTrigger=PropertyChanged}"/>

            <ui:Button Content="Применить фильтр" Appearance="Secondary" Margin="10 0 0 0" Command="{Binding ApplyFilterCommand}"/>
            <ui:Button Content="Сбросить" Appearance="Secondary" Margin="10 0 0 0" Command="{Binding ResetFilterCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.CategoryName}" Width="Auto"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="Auto"/>
                <DataGridTextColumn Header="Ед. Изм." Binding="{Binding UnitOfMeasure}" Width="Auto"/>
                <DataGridTextColumn Header="Цена за ед." Binding="{Binding UnitPrice}" Width="Auto"/>
                <DataGridTextColumn Header="Последняя поставка" Binding="{Binding LastSupplyDate, StringFormat='dd.MM.yyyy'}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" VerticalAlignment="Bottom" Margin="0 10 0 0"/>
    </Grid>
</UserControl>